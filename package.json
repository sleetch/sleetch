{
  "name": "ladoc",
  "scripts": {
    "build": "bunx bun-workspaces run build --dep-order --output-style=prefixed",
    "dev": "bunx bun-workspaces run dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-workspaces": "^1.11.2",
    "publint": "^0.3.22",
    "tsdown": "^0.22.14"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "./packages/*",
    "./examples/*"
  ]
}

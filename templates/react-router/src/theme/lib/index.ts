export { ThemeProvider } from '@/theme/lib/theme-provider';
export { useTheme } from '@/theme/hooks/use-theme';
export type { Theme } from '@/theme/types/themes';

export const storageKey = 'sleetch-theme';
